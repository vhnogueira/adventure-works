sources:
  - name: erp 
    database: fea25_05
    schema: raw_adventure_works
    tables:
      - name: sales_customer
        tests:
          - sql: |
              select *
              from {{ source('erp', 'sales_customer') }}
              where (personid is not null and storeid is not null)
                 or (personid is null and storeid is null)