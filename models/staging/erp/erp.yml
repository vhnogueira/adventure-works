sources:
  - name: erp 
    database: fea25_05
    schema: raw_adventure_works
    tables:
      - name: sales_customer
        tests:
          - dbt_utils.expression_is_true:
              expression: "not ((personid is not null and storeid is not null) or (personid is null and storeid is null))"